<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Page</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap');

        *
        {
            padding: 0;
            margin: 0;
        }

        nav
        {

            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 60px;
            box-shadow: 0px 0px 5px 0 rgba(0, 0, 0, .25);
        }

        nav h3
        {
            font-family: 'Roboto', sans-serif;
            color: rgb(41, 41, 41);
        }

        nav b
        {
            font-weight: 900;
        }

        nav a
        {   
            text-decoration: none;
            border: none;
            cursor: pointer;
            background-color: rgba(63,94,251,1) ;
            padding: 10px 20px;
            border-radius: 5px;
            color: white;
            font-family: 'Roboto', sans-serif;
            font-weight: 500;
            font-size: 15px;
        }

        form
        {
            display: flex;
            flex-direction: column;
            width: 25vw;
            margin: 1em;
        }

    </style>
</head>
<body>
    <nav>
        <h3>You're logged as <b><%= userName %></b>.</h3>
        <a href="/auth/logout">Log Out</a>        
    </nav>
    <div class="container">
        <h2>Registrar novo puzzle</h2>
        <form action="/enc/en" method="post">
                    <input type="text" name="name" placeholder="name" required>
                    <input type="number" name="uses_credit" placeholder="uses_credit" required>
                    <input type="number" name="credit_limit" placeholder="credit_limit">
                    <input type="number"name="closing_day" placeholder="closing_day">
                    <input type="number" name="expiration_day" placeholder="expiration_day">
            <button type="submit">Registrar</button>
        </form>
    </div>
</body>
</html>